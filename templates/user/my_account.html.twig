{% extends 'base.html.twig' %}

{% block title %}Mon Compte{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>👤 Mon Compte</h1>
    
    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h5>Mes Informations</h5>
                </div>
                <div class="card-body">
                    <p><strong>Email:</strong> {{ user.email }}</p>
                    <p><strong>Nom:</strong> {{ user.nom ?? 'Non défini' }}</p>
                    <p><strong>Prénom:</strong> {{ user.prenom ?? 'Non défini' }}</p>
                </div>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h5>Actions rapides</h5>
                </div>
                <div class="card-body">
                    <a href="{{ path('app_user_catalog') }}" class="btn btn-primary w-100 mb-2">
                        📚 Parcourir le catalogue
                    </a>
                    <a href="{{ path('app_logout') }}" class="btn btn-danger w-100">
                        🚪 Se déconnecter
                    </a>
                </div>
            </div>
        </div>
    </div>
    
    <div class="card mt-4">
        <div class="card-header bg-secondary text-white">
            <h5>Mes Emprunts</h5>
        </div>
        <div class="card-body">
            <div class="alert alert-info">
                Fonctionnalité à venir : liste de vos emprunts en cours et historique
            </div>
        </div>
    </div>
</div>
{% endblock %}